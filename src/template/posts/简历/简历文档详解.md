## 个人简介

> 吴华锦，深圳大学电子信息工程本科，13年+行业经验，资深前端开发工程师。精通Vue2/3、React、uni-app跨端开发，深度掌握音视频流媒体、硬件SDK对接、WebRTC实时通信技术，具备丰富的架构设计、系统重构与性能优化经验。深耕公安司法政企领域，主导多条核心产品线前端研发，覆盖20+省内外项目，具备项目全流程落地、跨部门协同与团队管理能力。

## 核心技能

> **核心技术栈、工程化/跨端/原生能力、音视频/实时通信/硬件对接、后端/国产化适配、项目管理与通用能力五大维度**

### 一、核心前端框架

1. **`Vue2/Vue3`(`Vue Router/Vuex/Pinia`)**

- 框架深度应用、版本迁移、组件封装、大型项目架构设计与重构
- 作为智能终端产品线前端技术负责人，主导了智能终端 `APP V4` 版本的技术栈升级，从 `Vue2` 迁移至 `Vue3`，并完成 `UI` 全量重构；
- 针对“监视事务管理”核心模块重写后，代码可维护性提升 `60%` 以上。
- 在远程会见三合一项目中，基于 `Vue2 + Vuex` 搭建架构，将 `3` 个独立系统整合为统一平台，代码复用率提升 `70%`，大幅降低维护成本。

2. **`React`(`UmiJS/Dva`)**

- 企业级 `React` 架构搭建、多版本适配、第三方库集成
- 智能视频行为分析平台项目中，基于 `React + UmiJS + Dva` 搭建企业级前端架构，采用 `Ant Design` 统一 `UI` 风格，封装公共模块与统一样式；
- 实现基础版和国产化适配版的并行开发，通过环境变量配置实现差异化功能加载，最终国产化版本成为现场部署的主流版本。

### 二、工程化/跨端/原生能力

1. **`uni-app`（`NVue、HTML5+ Runtime`）**

- 跨端开发（安卓大屏/平板/`PC`/`H5`）、原生渲染优化、离线数据处理
- 基于 `uni-app` 从 `0` 到 `1` 实现公安司法监所领域的安卓终端 `APP`、平板端离线/在线 `APP`、`PC` 端后台系统的跨端开发；
- 针对流媒体播放卡顿问题，用 `NVue` 替代普通 `Vue` 页面做原生渲染，结合原生插件实现视频对讲、相机预览的原生级性能，延迟降低 `80%` 以上；
- 集成 `SQLite` 本地数据库，实现平板端 `APP` 离线状态下的数据存储与同步，保障监所场景业务连续性。

2. **构建工具与工程规范（`Vite/Webpack、ESLint、TypeScript`）**

- 构建工具优化、`TS` 实战、代码规范落地、性能优化
- 在智能终端 `V4` 版本开发中，将构建工具从传统方案升级为 `Vite`，大幅提升开发热更新效率；
- 全量接入 `TypeScript` 做类型约束，结合 `ESLint` 统一代码规范，减少 `80%` 以上的类型相关线上 `bug`；
- 在多个项目中通过 `Webpack` 多分包、按需加载等优化，解决了旧系统页面渲染卡顿、动态 `Class` 卡死等问题。

3. **`Docker`容器化部署**

- 前端项目容器化、国产化环境适配部署
- 在智能视频行为分析平台项目中，完成前端项目的 `Docker` 容器化打包部署，适配国产化服务器环境，保障平台在统信 `UOS` 等系统下稳定运行；
- 结合 `Docker` 实现多环境（开发/测试/生成）的快速部署，提升项目交付效率 `30%` 以上。

4. **样式预处理器（`SCSS`/`Less`）**

- 样式模块化、统一样式体系搭建
- 在多个政企项目（如远程会见平台、行为分析平台）中，采用 `SCSS/Less` 搭建模块化样式体系，封装公共样式变量、混合器，实现 `UI` 风格统一，同时减少重复样式代码量约 `50%`，提升团队样式开发效率。

### 三、音视频/实时通信/硬件对接

1. **实时通信（`WebSocket/Socket.IO、WebRTC`）**

- 实时信令交换、音视频对讲、弱网适配、消息推送
- 基于 `Node.js + Express.js + Socket.IO` 搭建实时后端服务，实现信令远程会见平台的信令交换、文件传输、消息推送功能；
- 针对弱网环境下音视频卡顿问题，基于 `WebRTC` 实现自适应码率调整、丢包重传机制，将视频通话稳定性提升至 `95%` 以上；
- 在智能终端 `APP` 中集成 `WebSocket` 实现设备状态实时同步，保障监所场景下的实时性需求。

2. **流媒体与播放器（`HLS、RTSP、video.js、Jessibuca`）**

- 多格式流适配、跨端浏览器兼容、硬件播放器集成
- 在智能视频行为分析平台中，集成 `video.js`、`Jessibuca` 和海康视频插件，适配 `HLS` 流、`RTSP` 流等多种流媒体格式，解决了不同浏览器（`Chrome`/统信 `UOS` 浏览器）下播放兼容性问题；
- 优化播放器加载逻辑，将视频首屏加载时间缩短 `60%`。

1. **硬件 `SDK` 对接**

- 多厂商 `SDK` 集成、统一适配层封装、外设调用优化
- 封装统一的硬件适配层，解决海康、大华、来邦等多厂商 `SDK/API` 差异问题，快速集成人脸/指纹识别、串口通信等能力；
- 针对高拍仪、电子签名板等外设，采用单例模式 + `Promise` 化封装 `SDK` 调用，设计断线重连、超时控制、错误分级处理和日志机制，将业务调用复杂度降低 `60%` 以上，支撑数十个监所项目的硬件对接需求。

### 四、后端/国产化适配

1. **`Node.js/Express.js`**

- 轻量级后端服务开发、实时业务支撑
- 为远程会见三合一平台搭建 `Node.js + Express.js` 实时后端服务，完成信令交换、文件传输、静态文件服务器等功能开发，配合前端实现音视频对讲的实时性需求，支撑平台日均千级的并发请求处理，无服务宕机情况。

2. **国产化适配（达梦/人大金仓、统信 `UOS`）**

- 国产化数据库/浏览器适配、全栈国产化方案落地
- 在智能视频行为分析平台项目中，完成国产化数据库（达梦/人大金仓）、国产化浏览器（统信 `UOS`）的全链路适配，解决了国产环境下的兼容性、性能问题，最终国产化版本成为现场部署的主流按本，覆盖全国 `20+` 监所项目。

### 五、项目管理与通用能力

1. **项目重构与性能优化**

- 老旧系统重构、性能瓶颈突破、多系统整合
- 全面接手远程会见平台历史项目时，该项目架构混乱、性能卡顿，主导了架构调整、代码重构与性能优化，完成 `70%` 以上的功能迭代，推动 `3` 个独立系统整合为“三合一”平台，优化后页面加载速度提升 `80%`，客户反馈满意度提升 `90%`。

2. **全流程项目交付与招投标支撑**

- 招投标技术支持、全流程项目管理（开发/测试/部署/终验）、大型展会技术支撑。
- 为公安司法领域 `20+` 重大项目提供招投标技术支持，主导或核心参与 `20+` 项目的终验工作；
- 多次支撑全国性警博会的技术展示，保障智能终端、远程会见等核心产品的现场演示零故障，助力公司拿下了多个省内外监所项目。

3. **团队管理与跨部门协同**

- 新员工培训、跨部门资源协调、多版本并行开发管理
- 担任组态设计组组长和部门经理助理期间，完成新员工培训体系搭建，累计培训 `20+` 新员工；
- 在智能终端、行为分析平台等项目中，负责跨部门（产品/后端/硬件/客户）协同，协调资源解决硬件 `SDK` 对接、国产化适配等跨领域问题；
- 管理多个项目的多版本并行开发，保障基础版、国产化版、定制版同步迭代无代码冲突。
